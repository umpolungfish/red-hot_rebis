# The one split composition cannot decide

The sign of ∈ − ∋ fixes the verdict wherever it is nonzero: positive is B,
negative is F, on all 65 words of the sweep (01). At zero the sign says
nothing, and the corpus splits 9 T against 13 N with identical composition —
same counts, different verdict. Composition is exhausted here.

03 holds every balance-zero word of the sweep with its verdict and its fork
count. ⊢∈⊤∋⊣ is T; the insulin B chain, also ∈=∋=1, is N.
