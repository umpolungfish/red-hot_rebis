# The over-fusion is +4, uniform, and inert

The over-fusion ob3ect (0705e449, grounded full, Lean-verified) typed the fix as a
mesoscale ∈ — a motif-level split — whose addition closes the fold on an integer
winding. Quantified against the four pbp2a-binder variants:

    variant  verdict  ∈  ∋  surplus (∋−∈)
    s179     T        4  4  0
    s316     F        3  7  +4
    s453     F        3  7  +4
    s590     F        2  6  +4

The closing variant is balanced — four forks, four fuses. All three failing
variants carry the SAME surplus, +4: four more fuses than forks. The fix is not "a
division" but exactly four, uniform across the three.

And the surplus is inert. `banked` on s590, the worst: VACUOUS — no clear ever
fired against a live register, 0 deposits, 37 steps inert after a fixation. The
surplus fuses merge parts that were never split, so they do no work; they are empty
merges. That is what over-fusion IS at the fold level — not too much binding, but
binding of what was never divided.

The design directive, concrete: bring each failing variant to four forks matching
its fuses (add the ∈ splits s179 has and these lack, or remove the four empty ∋).
The closing variant is the template — its four fork/fuse pairs are the balance the
others miss.
