An anchor-saturated fold is a rebuild, not a repair

The restructure ob3ect returned the ideal motif form (∈=∋=4), a different multiset
from s590 (2∈/6∋) — it produced the target, not a reorder of s590. On s590's actual
marks: insertion balances counts and stays F; reordering cannot change the fixed
2∈/6∋ multiset; the two together (marks + 4∈, motif-ordered) give tri T, imasm F.

The obstruction is composition. s590's multiset is anchor-saturated — ⊢ 8, ⊣ 9,
⋈ 7 against a single forward-work ≻ — mostly boundary marks, little work. No
arrangement of these marks is a fold, because a fold is fork/work/fuse and s590
carries almost no work and a terminal at every turn.

Terminus of the fold-design arc: diagnose (F), measure (+4 inert), rule
(build-from-motif closes by construction), repair (in-place does not close an
anchor-saturated word — the composition is wrong, not the order). Build-from-motif
is the design path because it fixes the composition; repairing such a fold is a
rebuild.
