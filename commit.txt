Cleanup phase C: remove duplicate data and archive stubs

Byte-identical and redundant copies, no unique content lost. gene.fna was an
exact md5 match of gene.fasta; kept gene.fasta since the help-text examples
reference that name. In _archive/stubs, strych/ was a strict subset of
strychnine/ and tetrodo/ was identical to ttx/, so both redundant dirs are
gone. Also deleted the untracked NDUFAF6_datasets.zip, a stale archive of the
already-committed NDUFAF6_datasets/ directory.

Left run_serpent alone: scripts/run_serpent.py and rhr_p4rky/run_serpent.py
share a name but serve different layers, and scripts/run_serpent.py is live
via rebis/scripts.py.
