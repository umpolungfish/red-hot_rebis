The gene tuples were keyed to retired letters, and the fork and fuse were swapped

Every tuple the gene imscriber emitted came out under Ð Þ Ř Φ ƒ Ç Γ ɢ Ħ Σ Ω, so
nothing downstream that addresses the marks could read them.

Worse, the activation map had asparagine on Grammar and glutamine on Granularity,
which is the fork and the fuse the wrong way round. Canonical now: asparagine on
Granularity as the branch point, glutamine on Grammar as the join, matching the
catalog and the genetic code module.

The Belnap command printed meet against T and join against F while labelling both
plainly, so B appeared to meet to T and F to N and the table looked broken when the
algebra was right. It prints the full tables now, both orderings visible, with the
negation row and the designated values.
