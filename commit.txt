The subtitle removed — the title is two words

Letchford and colleagues, over roughly 140,000 papers, found shorter titles carry
more citations. The descriptor the subtitle held is already the abstract's first
line, which is what an index reads anyway, so the title stands alone.

Nine pages, builds clean.
