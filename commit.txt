At balance, closure is a non-trivial loop

The balance-zero ob3ect named the discriminant a non-trivial winding number — a
loop not deformable to a point. Measured across all 22 balance-zero words of the
sweep, that is exactly the split: N is no loop (11 words) or a loop enclosing
nothing (insulin B chain, ⋈∈∋…), T is at least one fork/fuse pair enclosing work
(9 words).

The condition is existential. 3I40 is insulin A and B concatenated, carries the B
chain's empty loop among its three, and reads T because the others enclose work.

Confirmed fresh on both engines: ⊢∈∋⊣ N, ⊢∈⊤∋⊣ T, ⊢∈∋∈∋⊣ N, ⊢∈∋∈⊤∋⊣ T, and
⊢⋈∈∋⊙◻⋈⊣ N — six marks of work in the word, all outside the loop, no closure.
Only what the loop encloses counts.

The fold law complete: sign of ∈−∋ positive is B, negative is F; at zero, T if some
pair encloses a mark, else N. N is not a failed fold but a fold that never opened.
