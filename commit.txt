The advertised commands are the commands that exist.

The demo list was hardcoded and wrong in both directions, offering a ligand demo that was never implemented while omitting three that were; it and demo all now derive from the module, so all really is all. The manual pointed at an alchemy map and a materials sim that live only in prose: they are now treatise and the real materials suites, and the SIC-POVM line points at the p4ra probe that actually carries it.
