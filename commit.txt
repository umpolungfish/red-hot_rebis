The balanced-fold rule is a repeat unit

The balanced-template ob3ect answered the converse of the over-fusion diagnosis:
the word is a repeat unit, ⊢[∈≻⊤≺⊥∋⋈]×4⊙⊞◻⊣, four balanced motifs. Each ∈ split
pairs with its own ∋ fuse one motif later, with both arms worked between them
(forward/true/reverse/false). A division pairs with its own merge, and the merge
fires only after both arms did work — exactly what the failing variants lacked,
their surplus fuses firing inert on unforked parts.

Balance holds by repeating the unit: growth adds paired ∈/∋ motifs, keeping a 1:1
fork/fuse ratio at every size, typed as ⊞=𐑙, the self-referential stoichiometry
limit. Structural, held by construction, not tested after.

This closes the fold-design arc — diagnose (F), measure (+4 inert surplus), fix
(add splits), rule (build from the motif) — turning the sweep from auditor into
designer.
