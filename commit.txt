The seam completes the glycopeptide — bulk and boundary as one T-open object

Prompt 3, the last rung (ob3ect 67a07fb5, grounded full, Lean-verified). The
peptide–glycan bond reads as ⊢∈≻⊤≺⊥⋈⊙⊞◻∋⊣, and its ∈/∋ pair BRACKETS the whole
word rather than sitting mid-word: the split is dual_word_genesis → [peptide word,
glycan word], the fuse is the glycopeptide recognition unit. The seam makes the two
separately-typed words the two arms of one fork-fuse — the glycopeptide is a single
object whose δ produces both and whose μ reunites them, not a chain with the glycan
appended.

What the join carries that neither word has alone, from the marks it forces:
⊙ (criticality) — a scale-free functional sensitivity emergent at the merge that
neither component possesses; and ∋ (conjunctive) — the recognition unit needs both
present at once.

The verdict is T over an open walk, the same closure the glycan carried. The seam
does NOT close the walk, and that is correct: a glycopeptide is still a recognition
boundary reaching outward, so the bond must keep it environment-facing rather than
seal it inward. This corrects the earlier prediction that the seam would supply the
glycan's missing return — it supplies unification and emergent criticality, not
closure.

The boundary interface is now characterized end to end: peptide bulk, glyco
interface, glycan boundary, eight monosaccharide types, and the seam — four layers,
each grounded and Lean-verified, one T-open recognition object at every scale.
