Cleanup phase E: relocate misplaced root files into their subdirs

The repo root had collected loose data and image files that belong in the
existing topic dirs. No code reads any of them by path (verified by grep), so
these are pure relocations: gene.fasta and rcsb_pdb_1MBB.fasta into fasta/,
2.png and rebis1.png into images/, bevy_results.json and Ligand.txt (a ligand
generation log) into data/, and the sourceless sic_povm build PDF into
materials/. The root now holds only the canonical launcher, packaging, and
docs.
