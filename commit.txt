Fold sweep: 65 red-hot_rebis folds verdicted through Vox

Every PDB in the repo lifted through vox pdb and verdicted. 68 files, 65
sequence-bearing (3 are poly-alanine scaffolds, no word). Distribution: T 9, B 22,
F 21, N 13.

What closes are the small natural stable folds — Trp-cage, villin, insulin, the
insulin A-chain, beta-endorphin — plus two designs, pbp2a_binder_s179 and PSII_D1.
The minimal folds nature settled close; most designs sit at B or F.

The actionable read is F, over-fused: HUMAN_INSULIN and three of four pbp2a_binder
variants merge more than they split, and banked/insert on the word say whether the
surplus is exposed and what one change balances it. N (mostly the DARPin and odot
designs) is too little promoted structure to open a fork — a recognition scaffold
with no typed interior. The pbp2a_binder series is the sharpest single result: s179
closes T where s316/s453/s590 are F, four variants of one binder discriminated by
the sweep. Table in pdb_fold_sweep.tsv.
