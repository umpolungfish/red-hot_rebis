Cleanup phase F: archive orphaned experiment dirs

plasma/, unified_demo/, Ars_Therapeutica/, and natural_products/ are not
imported by the rebis facade or any wired sibling (plasma and unified_demo
only self-import; natural_products has no .py at all and was already in the
package-discovery skip set). Moved them under _archive/ rather than deleting,
so nothing is lost while the top level clears up. Packaging still lists them;
that is reconciled in the pyproject consolidation phase.

Kept demo_scripts/ in place despite the plan flagging it: rebis/demo.py runs
_demo_*.py from it at runtime, so it is live infrastructure, not superseded.
