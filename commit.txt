The glycan tree closes T-but-open — the closure a boundary carries

Prompt 2 (ob3ect f5e79dfa, grounded full, Lean-verified, O₂dag). The branched
sugar tree reads as one word, ⊢⊙≻⋈⊙∈≻⋈⊙⊣≻⋈⊙⊣∋⊤⊥⊞◻⊣, and it is the biantennary
N-glycan built structurally: reducing-end anchor, a single ∈ branch point at
position 6 splitting into two antennae that each run to a terminal leaf, then ∋
fusing them into the receptor binding site. The one ∈/∋ pair IS the branching —
the fork/fuse the prompt asked for, fallen out of the structure.

The closure is the finding: tri-ancestral verdict T over an OPEN walk —
reconnection without return. The antennae fuse into a recognition surface (the T)
but the word does not return to its start (open). That is what a boundary
interface is: environment-facing, reaching outward to bind and not closing on
itself. A glycan that closed its walk would be a molecule turned inward, which a
recognition boundary is not.

Consistent across scales: the fuse ∋ is the receptor binding site, and in prompt 1
the terminal environment-facing sugar NeuAc was the only monosaccharide carrying
∈, the recognition gate. Recognition marks the boundary's outer edge at both the
residue and the whole-tree level.

The stack is now complete — peptide bulk (vox aa/fasta/pdb), interface seam
(vox glyco), glycan boundary (this), monosaccharides (fg marks + ⊥ chirality word,
eight types). The boundary interface is characterized end to end.
