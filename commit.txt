The census is a sign, not a proportion

The composition-census ob3ect typed the diagnostic as a boundary/work ratio with a
critical threshold. Measured across the 65-word PDB sweep, no definition of
boundary versus work separates the verdicts — T, F and N ranges overlap on all
three tried.

The sign of ∈ − ∋ separates them exactly: positive is B (22/22), negative is F
(21/21), zero is T or N (9/13). Magnitude carries nothing; B runs +1 to +36 and F
runs −1 to −5 with the verdict constant across each range. Surplus fork is B,
surplus fuse is F, balance is the entry condition for closure.

Balance is necessary for T, not sufficient — ∈=∋=0 is always N, and the T/N split
at ∈=∋≥1 is decided by arrangement, not composition.

This corrects the anchor-saturation reading: s590 is F because ∈−∋ = −4, not
because its anchors outweigh its work. Anchor-heavy words appear at every verdict.
The rebuild conclusion is unchanged — insertion cannot fix the sign under cyclic
pairing and reordering cannot change a multiset's sign at all.
