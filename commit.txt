Add COMMANDS.md: the command grimoire

A slick, single-glance reference for the full command surface, written in the
toolchain's own register. Frames rebis.chain as the ouroboros that closes DNA
through retrosynthesis, with every Tier I/II engine as one arc of that loop,
plus the Python namespace face, the make targets, and the reference submenu.
All 18 entry points, accurate to pyproject. Shipped via MANIFEST.in.
