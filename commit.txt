The compilation from RNA to protein, stated exactly

Every stage now has a closed form. Nucleotide→B₄ (G=B, C=T, A=F, U=N), codon→amino
acid by the Lean-proved genetic code, twelve promoted amino acids bijecting the
twelve axes (23 of 61 sense codons promoted, 38 silent), and — new — word→verdict
by the sign law and the enclosure law.

In codons the fold law is about two families: ∈ is Asn = AAY, ∋ is Gln = CAR. They
differ in the first base alone, A forks and C fuses, with A fixed at the second
position. Count AAY against CAR in the ORF: AAY > CAR is B, CAR > AAY is F, equal
closes T exactly when some AAY is separated from its matching CAR by at least one
other promoted codon, and is N otherwise.

Verified on nine sweep PDBs: sequence from the CA trace, back-translated, lifted
through vox --rna, verdicted by the law. Nine of nine agree with the structure-lane
sweep and with vox's own engine. Trp-cage is the cleanest — the transcript gives
⊢∈≺⊤∋⊣⊞⊥ once, the PDB gives the motif thirty-eight times, once per NMR model.

Folding adds nothing the sequence lacked. The verdict is computed from the promoted
subsequence alone, so the fold is decided at translation.
