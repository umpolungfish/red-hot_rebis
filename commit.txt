Cleanup phase D: untrack external patent PDFs

The three patent documents under _archive/ (US11724831, US11818829,
WO2020159603A2, 5.6M total) are external reference material, not project
artifacts, and are retrievable from the patent offices if ever needed. Removed
them from tracking and added _archive/*.pdf to gitignore; the files stay on
disk. Per the chosen approach this does not rewrite history, so .git keeps its
existing size; the point is to stop tracking bulk binaries going forward.

Kept the sic_povm build PDF (moved to materials/) tracked, since it has no
source in-tree and could not be regenerated, and left pdfs/ figures tracked
for the same reason.
