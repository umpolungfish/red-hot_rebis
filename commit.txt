Over-fusion quantified: +4 uniform, and inert

The over-fusion ob3ect typed the fix as a mesoscale ∈ split closing the fold on an
integer winding. Measured against the four pbp2a-binder variants: the closing one
(s179) is balanced 4∈/4∋; all three failing variants carry the SAME +4 surplus of
fuses over forks. The fix is exactly four splits, uniform across the three, not "a
division."

banked on the worst (s590) reads VACUOUS — no clear against a live register, 0
deposits, 37 inert steps. The surplus fuses merge parts that were never split, so
they do no work. Over-fusion at the fold level is empty merging, not excess
binding. s179's four fork/fuse pairs are the template the others miss.
