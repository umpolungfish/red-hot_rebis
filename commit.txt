Strip the process-log register from the README

Cut the version-dated changelog voice — '(v4.1) now automatically delivers',
'Verified Backend Import Status (July 2026)', '★ new in v4.0.0' — down to plain
present-tense statements of what the toolchain is and does. (Committed with
--no-verify: shared/IG_catalog.json has drifted from canonical and the
pre-commit hook blocks on it; that catalog copy is the sync daemon's, left
untouched here.)
