from setuptools import setup, find_packages

setup(
    name="p4ramill",
    version="0.1.0",
    description="Python mirror of the p4ramill Lean 4 paraconsistent kernel",
    packages=find_packages(),
    python_requires=">=3.10",
)
